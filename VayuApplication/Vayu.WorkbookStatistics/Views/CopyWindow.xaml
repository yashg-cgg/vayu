<Window    x:Class="Vayu.WorkbookStatistics.Views.CopyWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"     
                   Title="Vayu - Copy" Height="125"      Width="242"  ResizeMode="NoResize"  WindowStartupLocation="CenterScreen"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
         
        <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay}" HorizontalAlignment="Left" Margin="20,10,0,0" VerticalAlignment="Top"/>
        <Button Content="OK" Command="{Binding CopyCommand}" Click="CancelButton_Click" HorizontalAlignment="Left" Margin="10,59,0,0" VerticalAlignment="Top" Width="50"/>
        <Button Content="Cancel" Click="CancelButton_Click" HorizontalAlignment="Left" Margin="162,59,0,0" VerticalAlignment="Top" Width="50"/>
        <RadioButton Content="All" IsChecked="{Binding AllChecked, Mode=TwoWay}" HorizontalAlignment="Left" Margin="151,10,0,0" VerticalAlignment="Top"/>
        <RadioButton Content="Selected" IsChecked="{Binding SelectedChecked, Mode=TwoWay}" HorizontalAlignment="Left" Margin="151,31,0,0" VerticalAlignment="Top"/>
    </Grid>
</Window>
