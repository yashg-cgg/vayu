<Window    x:Class="Vayu.WorkbookStatistics.Views.ConstraintDollarCheck"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/" xmlns:local="clr-namespace:Vayu.WorkbookStatistics.Views"
        prism:ViewModelLocator.AutoWireViewModel="True"  Title="Vayu - ConstraintDollarCheck"       Height="800" Width="1500">
    <Window.Resources>
        <ResourceDictionary>
             
            <local:DGDollarCellForeColorConverter x:Key="myFGColor"/>
            <local:DGDollarCellBackColorConverter x:Key="myBGColor"/>
            <Style x:Key="CenterCellStyle" TargetType="{x:Type DataGridCell}">
                <Setter Property="Foreground">
                    <Setter.Value>
                        <MultiBinding Converter="{StaticResource myFGColor}">
                            <Binding RelativeSource="{RelativeSource Self}" />
                            <Binding RelativeSource="{RelativeSource Self}" Path="Content.Text" />
                        </MultiBinding>
                    </Setter.Value>
                </Setter>
                <Setter Property="Background">
                    <Setter.Value>
                        <MultiBinding Converter="{StaticResource myBGColor}">
                            <Binding RelativeSource="{RelativeSource Self}" />
                            <Binding RelativeSource="{RelativeSource Self}" Path="Content.Text" />
                            <Binding RelativeSource="{RelativeSource FindAncestor, AncestorType={x:Type DataGridRow}}" Path="." />
                            <Binding Path="."/>
                        </MultiBinding>
                    </Setter.Value>
                </Setter>
                <Setter Property="Margin" Value="0,0,0,0"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type DataGridCell}">
                            <Grid Background="{TemplateBinding Background}" >
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Content="Override and Submit" Height="30" Width="140" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5,2,0,0" Command="{Binding OverrideCommand}" FontWeight="Bold"/>
        </StackPanel>
        <DataGrid Grid.Row="1" Margin="0,10,0,0"  VerticalAlignment="Stretch" ItemsSource="{Binding ExposureConstraintList, Mode=TwoWay}" CanUserAddRows="False" AreRowDetailsFrozen="True" 
                  AutoGenerateColumns="False" SelectedItem="{Binding SelectedConstraintPathValue, Mode=TwoWay}" SelectionMode="Single" SelectionUnit="FullRow" AlternationCount="2" 
            VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling" ScrollViewer.IsDeferredScrollingEnabled="True" HorizontalAlignment="Stretch" ScrollViewer.CanContentScroll="True">
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Style.Triggers>
                        <Trigger Property="AlternationIndex" Value="0">
                            <Setter Property="Background" Value="White" />
                        </Trigger>
                        <Trigger Property="AlternationIndex" Value="1">
                            <Setter Property="Background" Value="LightGray" />
                        </Trigger>
                        <DataTrigger Binding="{Binding IsShiftEmpty}" Value="false">
                            <Setter Property="Background" Value="Teal" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="BlueViolet" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.ContextMenu>
                <ContextMenu >
                    <MenuItem Header="Path Details" Command="{Binding RunRetrieveRightClickCommand}"  />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding ID}" CanUserReorder="False" FontWeight="Bold" Header="ID" IsReadOnly="True" Width="50*" MinWidth="40" />
                <DataGridTextColumn Binding="{Binding Constraint}" CanUserReorder="False" FontWeight="Bold" Header="Constraint" IsReadOnly="True" Width="150*" MinWidth="40" />
                <DataGridTextColumn Binding="{Binding Contingency}" CanUserReorder="False" FontWeight="Bold" Header="L/O" IsReadOnly="True" Width="150*" MinWidth="40" />
                <DataGridTextColumn Binding="{Binding HE1,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="1" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}" />
                <DataGridTextColumn Binding="{Binding HE2,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="2" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE3,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="3" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE4,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="4" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE5,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="5" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE6,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="6" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE7,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="7" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE8,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="8" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE9,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="9" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE10,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="10" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE11,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="11" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE12,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="12" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE13,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="13" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE14,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="14" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE15,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="15" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE16,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="16" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE17,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="17" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE18,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="18" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE19,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="19" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE20,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="20" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE21,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="21" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE22,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="22" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE23,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="23" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding HE24,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="24" IsReadOnly="True" Width="50*" MinWidth="40" CellStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Binding="{Binding Sum,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="Total" IsReadOnly="True" Width="55*" MinWidth="40"/>
                <DataGridTextColumn Binding="{Binding RiskType,StringFormat={}\{0:N0\}}" CanUserReorder="False" Header="Type" IsReadOnly="True" Width="55*" MinWidth="40" />
                <DataGridTextColumn Binding="{Binding Shift}" CanUserReorder="False" Header="Shift" IsReadOnly="True" Width="50*" MinWidth="40" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
