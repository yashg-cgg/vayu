<Window x:Class="Vayu.CRRPNLDetails.Views.PathExposureDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:vm ="clr-namespace:Vayu.CRRPNLDetails.ViewModels"
        xmlns:local="clr-namespace:Vayu.CRRPNLDetails.Views"
        mc:Ignorable="d"
        Title="PathExposureDetails" Height="450" Width="800">
    <Window.Resources>
        <vm:DataGridBackColorConverter x:Key="myBGColor"/>
        <Style x:Key="CenterCellStyleBG" TargetType="{x:Type DataGridCell}">
            <Setter Property="Foreground" 
                        Value="{Binding RelativeSource={RelativeSource Self},
                        Path=Content.Text, 
                        Converter={StaticResource myBGColor}}" />
        </Style>
    </Window.Resources>

    <Grid>
        <DataGrid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ItemsSource="{Binding PathwiseExposureList}" AutoGenerateColumns="False" AlternationCount="2" 
                  CanUserAddRows="False">
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Style.Triggers>
                        <Trigger Property="AlternationIndex" Value="0">
                            <Setter Property="Background" Value="White" />
                        </Trigger>
                        <Trigger Property="AlternationIndex" Value="1">
                            <Setter Property="Background" Value="LightGray" />
                        </Trigger>

                    </Style.Triggers>
                    <Setter Property="Height" Value="23"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Participant" Width="100"  Binding="{Binding Participant , Mode=TwoWay}"  />
                <DataGridTextColumn Header="Auction" Width="100"  Binding="{Binding Auction , Mode=TwoWay}"  />

                <DataGridTextColumn Header="Source" Width="100"  Binding="{Binding Source , Mode=TwoWay}"  />
                <DataGridTextColumn Header="Sink" Width="200"  Binding="{Binding Sink , Mode=TwoWay}"    />
                <DataGridTextColumn Header="ClassType" Width="100" Binding="{Binding ClassType , Mode=TwoWay}"  />
                <DataGridTextColumn Header="TotalMW" Width="100" Binding="{Binding MW , Mode=TwoWay}"  />
                <DataGridTextColumn Header="TotalMWExposures"  Width="*"   Binding="{Binding TotalMWExposure , Mode=TwoWay , StringFormat=0.00}" CellStyle="{StaticResource CenterCellStyleBG}" />
                <DataGridTextColumn Header="TotalDollarExposures" Width="*"   Binding="{Binding TotalDollarExposure , Mode=TwoWay , StringFormat=0.00}" CellStyle="{StaticResource CenterCellStyleBG}" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
