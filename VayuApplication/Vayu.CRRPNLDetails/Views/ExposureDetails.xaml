<Window x:Class="Vayu.CRRPNLDetails.Views.ExposureDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:vm ="clr-namespace:Vayu.CRRPNLDetails.ViewModels"
        xmlns:local="clr-namespace:Vayu.CRRPNLDetails.Views"
        mc:Ignorable="d"
        Title="ExposureDetails" >
    <Window.Resources>
        <vm:DataGridBackColorConverter x:Key="myBGColor"/>
        <Style x:Key="CenterCellStyleBG" TargetType="{x:Type DataGridCell}">
            <Setter Property="Foreground" 
                        Value="{Binding RelativeSource={RelativeSource Self},
                        Path=Content.Text, 
                        Converter={StaticResource myBGColor}}" />
        </Style>


    </Window.Resources>

    <Grid>
        <DataGrid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ItemsSource="{Binding FTRExposureList}" SelectedItem="{Binding SelectedConstraintPathValue , Mode=TwoWay}" AutoGenerateColumns="False" AlternationCount="2" 
                  CanUserAddRows="False">
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Style.Triggers>
                        <Trigger Property="AlternationIndex" Value="0">
                            <Setter Property="Background" Value="White" />
                        </Trigger>
                        <Trigger Property="AlternationIndex" Value="1">
                            <Setter Property="Background" Value="LightGray" />
                        </Trigger>

                    </Style.Triggers>
                    <Setter Property="Height" Value="23"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="{Binding RunPathDetailsCommand}" Header="Path Details" />
                    <MenuItem Command="{Binding RunHistoricalConstOpenCmd}" Header="Constrtaint History" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ConstraintId" Width="100"  Binding="{Binding ConstraintId , Mode=TwoWay}"  />
                <DataGridTextColumn Header="Constraint" Width="200"  Binding="{Binding Constraint , Mode=TwoWay}"    />
                <DataGridTextColumn Header="Contingency" Width="400" Binding="{Binding Contingency , Mode=TwoWay}"  />
                <DataGridTextColumn Header="TotalMWExposures"  Width="100"   Binding="{Binding TotalMWExposure , Mode=TwoWay , StringFormat=0.00}" CellStyle="{StaticResource CenterCellStyleBG}" />
                <DataGridTextColumn Header="TotalDollarExposures" Width="*"   Binding="{Binding TotalDollarExposure , Mode=TwoWay , StringFormat=0.00}" CellStyle="{StaticResource CenterCellStyleBG}" />
                <DataGridTextColumn Header="PeakMWExp" Width="*"   Binding="{Binding PeakMWExposure , Mode=TwoWay , StringFormat=0.00}" CellStyle="{StaticResource CenterCellStyleBG}" />
                <DataGridTextColumn Header="OffPeakMWExp" Width="*"   Binding="{Binding OffPeakMWExposure , Mode=TwoWay , StringFormat=0.00}" CellStyle="{StaticResource CenterCellStyleBG}" />
                <DataGridTextColumn Header="PeakDollarExp" Width="*"   Binding="{Binding PeakDollarExposure , Mode=TwoWay , StringFormat=0.00}" CellStyle="{StaticResource CenterCellStyleBG}" />
                <DataGridTextColumn Header="OffPeakDollarExp" Width="*"   Binding="{Binding OffPeakDollarExposure , Mode=TwoWay , StringFormat=0.00}" CellStyle="{StaticResource CenterCellStyleBG}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
