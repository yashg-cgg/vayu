<UserControl x:Class="Vayu.PowerMap.Controls.CheckBoxListBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Vayu.PowerMap.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="115" d:DesignWidth="124">
    <Grid>
        <Border CornerRadius="15">
            <Grid >
                <Grid.RowDefinitions>
                    <RowDefinition Height="200*" />
                    <RowDefinition Height="22" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <ListBox Name="ListBox" IsHitTestVisible="True" IsManipulationEnabled="False" Grid.Row="0" Grid.ColumnSpan="2">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Name="CheckBox" IsChecked="{Binding Path=IsChecked}" Content="{Binding Path=Text}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Button Name ="All_CheckBox" Content="All" Click="All_CheckBox_Click" Grid.Row="1" />
                <Button Name ="None_CheckBox" Grid.Column="1" Content="None" Click="None_CheckBox_Click"  Grid.Row="1" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
